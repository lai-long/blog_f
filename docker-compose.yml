services:
  web:
    build: ./web
    container_name: myblog-web
    restart: unless-stopped
    ports:
      - "80:80"     # 标准 HTTP 端口，访问不用带端口号
      - "8080:80"   # 保留作测试用
    extra_hosts:
      # 让容器里的 nginx 能访问宿主机的后端（localhost:8812）
      - "host.docker.internal:host-gateway"
